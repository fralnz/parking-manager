import { createClient } from '@supabase/supabase-js';

/* const supabaseUrl = ''; // Sostituisci con il tuo URL Supabase
const supabaseKey = 'your-anon-key'; // Sostituisci con la tua chiave anonima

export const supabase = createClient(supabaseUrl, supabaseKey); */